version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:latest
    steps:
      - checkout
      - run:
          name: Install MinGW for cross-compile
          command: sudo apt -y install binutils-mingw-w64 gcc-mingw-w64 gcc-multilib
      - run:
          name: Greeting
          command: echo "A first hello"
